type Person @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  lastname: String!
  checkins: [Checkins] @hasMany
}

type Checkins @model @auth(rules: [{allow: public}]) {
  id: ID!
  position: String!
  date: AWSDate!
  time: AWSTime!
}
 